{
  "name": "sample-app",
  "version": "1.0.0",
  "description": "Sample Node.js/Express app used to demonstrate a full GitHub Actions CI/CD pipeline",
  "main": "src/index.js",
  "license": "MIT",
  "scripts": {
    "start": "node src/index.js",
    "lint": "eslint src --ext .js",
    "test": "jest --coverage",
    "build": "echo 'No transpilation needed for this sample - packaging into a container image instead' && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
